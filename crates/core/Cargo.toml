[package]
name = "agent-world-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core types and event store for AgentWorld"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
