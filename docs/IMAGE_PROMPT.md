# Hero Image Prompt

Generate with ChatGPT DALL-E or NanoBanana. Save result as `docs/hero.png`, then uncomment the image line in README.md.

## Prompt

```
A dark, moody 2D top-down view of a digital workspace. Five glowing geometric entities (small colored squares — blue, green, orange, pink, purple) are scattered across a dark grid floor divided into three distinct rooms connected by pulsing purple portals. Golden arcing projectiles travel between the entities like visible messages. Small thought bubbles float above two of the entities. Faint dotted trails show where the entities have moved. The scene has a subtle sci-fi aesthetic — dark navy background, soft neon glow on the entities, thin blue grid lines. No text, no UI elements, just the spatial world itself. Style: clean vector-like digital art, similar to a strategy game minimap or circuit board visualization. Aspect ratio 16:9, dark theme.
```

## After generating

1. Save as `docs/hero.png` (or `docs/hero.jpg`)
2. Edit README.md — uncomment the image line near the top:
   ```
   ![AgentWorld Screenshot](docs/hero.png)
   ```
3. Commit and push
